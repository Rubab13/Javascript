// A function that checks whether a number is positive, negative or zero.

function checkNumber (number) {
  number>=0 ? console.log(`The number ${number} is a positive number.`) : console.log(`The number ${number} is a negative number.`);
}

checkNumber(8);
checkNumber(0);
checkNumber(-1);
checkNumber(-12987234);